# nodes 三项目使用教程

本教程覆盖本仓库里 nodes 玩法的三个项目,它们构成一条完整的链路:

| 项目 | 目录 | 作用 | 语言/技术 |
| --- | --- | --- | --- |
| **nodes 主插件** | `nodes/` | 游戏内领土/资源/港口玩法与数据存储 | Kotlin(Paper) |
| **nodes-squaremap 附属** | `nodes-squaremap/` | 把 nodes 状态渲染到 squaremap 地图 | Java(Paper 插件) |
| **squaremap 编辑器** | `squaremap/` | 网页上查看/绘制领土、管理资源并导出 world.json | React + Leaflet + 纯 JS |

工作流程:主插件在游戏内生成/保存数据 → 网页编辑器导出 `world.json` → 上传服务器
→ 主插件加载 → 附属渲染到 squaremap 地图。

> `dynmap/` 是本编辑器的原型(dynmap 版编辑器),仅作参考,部署请使用
> squaremap 系列。

---

## 一、nodes 主插件(nodes/)

### 简介
核心玩法插件:玩家可以圈领土、放置资源节点、建造港口、建立城镇与国家等。
数据以 JSON 形式存放在 `plugins/nodes/` 下,与外部工具(编辑器/附属)通过
**文件**耦合。

### 构建
要求:Java JDK 25
```
cd nodes
./gradlew build
```
产物:`build/libs/nodes-VERSION.jar`

### 安装
1. 把 jar 放入服务器 `plugins/` 目录。
2. 重启服务器(或 `/reload`),插件自动生成 `plugins/nodes/config.yml`。
3. 游戏内命令:`/nodes`(主命令,别名 `/nd`),玩法操作主要在游戏内 GUI 完成。

### 数据文件(重要)
`plugins/nodes/` 下的 JSON:
- `world.json` —— 领土 + 资源节点定义。**主插件从不自动创建**,由 squaremap
  网页编辑器导出后由管理员上传。
- `towns.json` / `ports.json` / `war.json` —— 城镇/港口/战争状态,主插件
  定时自动保存。
- `config.yml` —— 插件配置(费用、备份周期、地图渲染开关等)。

---

## 二、nodes-squaremap 附属(nodes-squaremap/)

### 简介
squaremap 地图插件(https://github.com/jpenilla/squaremap)的附属,把 nodes
状态渲染成地图标记:领土多边形(按城镇/国家着色,被占领领土用占领者颜色
描边)、资源节点图标、港口图标、城镇/国家名称文字精灵,每个 squaremap 世界
注册一个 "Nodes" 图层。

**节点详情**:地图上悬停资源节点图标,显示该资源的完整详情(名称 + 收入 /
矿物 / 作物 / 动物,中文标签);悬停领土显示名称/归属 + 资源节点列表。

### 构建与测试
要求:Java JDK 25;squaremap-api 1.3.11(自动从 papermc 仓库解析)
```
cd nodes-squaremap
./gradlew build        # 产物: build/libs/nodes-squaremap-0.0.1.jar
./gradlew test         # TerritoryOutline 单元测试
```

### 安装
1. 把 `nodes-squaremap-0.0.1.jar` 放入服务器 `plugins/`(squaremap 必须先装)。
2. 重启服务器。附属默认每 100 ticks(5 秒)检测 `plugins/nodes/*.json` 的
   mtime,**文件变化才重建标记**;同时把 JSON 复制到
   `plugins/squaremap/web/nodes/` 供编辑器读取。
3. 打开 squaremap 网页(默认 `http://<服务器>:8080/`),勾选 "Nodes" 图层。

### 常用配置(`plugins/nodes-squaremap/config.yml`)
| 配置项 | 默认 | 说明 |
| --- | --- | --- |
| `data-directory` | `plugins/nodes` | nodes 数据目录 |
| `update-interval` | `100` | 轮询间隔(ticks) |
| `world` | `auto` | 渲染的世界(`auto` = 全部,或列表) |
| `copy-json-to-web` | `true` | 复制 JSON 到 squaremap web 目录 |
| `render.territories` 等 | `true` | 领土/节点图标/港口/城镇名等渲染开关 |
| `icons.<类型>` | | 各类资源节点图标颜色 |

---

## 三、squaremap 编辑器(squaremap/)

### 简介
nodes 的网页编辑器/查看器,是 dynmap 编辑器(`dynmap/`)的 squaremap 移植版:
在 squaremap 瓦片地图上直接查看/绘制领土、管理资源节点、随机生成领土、管理
城镇与国家,最后导出 `world.json` 交给服务器加载。UI 全中文,几何模块是纯 JS
移植,**构建不需要 Rust**。

### 构建(不需要 Rust)
要求:node.js >= 18、npm
```
cd squaremap
npm install
npm run build          # 产物在 build/
npm test               # 几何模块单元测试(71 项)
```

### 部署
把 `build/` 下所有文件复制到服务器:
```
plugins/squaremap/web/nodes-editor/
```
然后浏览器打开:
- `http://<服务器>:8080/nodes-editor/editor.html` —— 编辑器
- `http://<服务器>:8080/nodes-editor/nodes.html` —— 只读查看页

> 前提:服务器已装 nodes-squaremap(自动把 JSON 复制到 `web/nodes/`),且
> `plugins/nodes/world.json` 存在(初始可为空文件)。

### 编辑器使用速览
面板右侧是 6 个页签:

1. **世界**:地图显示选项(资源图标/领土 ID/费用/纯色填充/边界/城镇名/国家名/
   首都开关)、城镇列表(可排序)、选中领土信息。
2. **领土**:创建/删除/合并领土;画笔绘制(空格开关键盘模式,右键拖动画区块,
   Ctrl+右键擦除,Shift+拖动或 `-`/`+` 按钮调画笔大小,按住 A 新建领土);
   重命名、添加/移除资源节点。
3. **资源**:全局资源节点库(名称/图标/属性 JSON:income/ore/crops/animals)。
4. **生成**:把大领土随机细分成多个小领土(平均半径/随机种子/平滑迭代等参数);
   按概率给选中领土随机分配资源。
5. **城镇**:城镇/国家管理(名称、颜色取色器、首都领土 ID、领土归属/占领操作、
   玩家管理),可下载 towns.json。
6. **选项**:领土费用参数、默认资源属性、背景图片。

### 保存与导出
编辑完成后点面板右上角**「保存」**,浏览器下载 `world.json`;把文件上传到
服务器覆盖 `plugins/nodes/world.json`(**UTF-8 无 BOM**)。

---

## 四、三项目联动:从零搭建

```
1. 安装 squaremap 插件                    → 服务器 plugins/ + 重启
2. 安装 nodes 主插件(nodes-VERSION.jar)   → 服务器 plugins/ + 重启
3. 安装 nodes-squaremap 附属              → 服务器 plugins/ + 重启
4. 构建并部署 squaremap 编辑器            → build/* 复制到 plugins/squaremap/web/nodes-editor/
5. 创建空 world.json 使主插件正常加载     → {"meta":{"type":"world"},"nodes":{},"territories":{}}
6. 打开编辑器 http://<服务器>:8080/nodes-editor/editor.html
7. 画领土 / 定义资源 / 管理城镇
8. 点「保存」导出 world.json → 上传覆盖 plugins/nodes/world.json(无 BOM)
9. 重启服务器,让主插件加载新世界
10. 打开 squaremap 主页,勾选 Nodes 图层:
    悬停节点图标看资源详情,悬停领土看归属与资源列表
```

> 之后每次用编辑器更新 `world.json` 并上传:附属每 5 秒检测到变化会自动刷新
> 地图标记,**无需再重启服务器**(主插件内玩法状态如需同步则重启)。

---

## 常见问题

- **编辑器页面空白**:强制刷新(Ctrl+Shift+R);确认 squaremap 已开内部 Web
  服务、瓦片地址为 `{z}/{x}_{y}.png` 格式。
- **主插件报 "Invalid world file"**:`plugins/nodes/world.json` 缺失或格式
  错误;确认文件存在且为 UTF-8 无 BOM 的合法 JSON。
- **地图没有 Nodes 图层**:确认 nodes-squaremap 已启用且日志出现
  "Hooked into squaremap"。
- **编辑器数据加载失败**:浏览器 F12 → Network,检查
  `/nodes-editor/js/nodes.js`、`/nodes-editor/nodes/resource_icons.json`、
  `/nodes/{config,world,towns,ports}.json` 是否 200。

---

更多细节见各项目内 README:
- `nodes/README.md`
- `nodes-squaremap/README.md`
- `squaremap/README.md`
